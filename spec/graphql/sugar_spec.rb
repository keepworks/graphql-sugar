require "spec_helper"

RSpec.describe GraphQL::Sugar do
  it "has a version number" do
    expect(GraphQL::Sugar::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
